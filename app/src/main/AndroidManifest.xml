<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.samuel.medimagem">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <uses-feature android:name="android.hardware.camera" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ResumoExameActivity"
            android:parentActivityName=".ExameActivity">

        </activity>
        <activity android:name=".CadastrarExameActivity"
            android:parentActivityName=".ExameActivity"
            android:label="Marcar Exame"/>
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ExameActivity" />
        <activity android:name=".MainActivity" />
        <activity
            android:name=".CameraActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".FotosActivity"
            android:parentActivityName=".CameraActivity" />
        <activity android:name=".LaudoActivity" />
        <activity android:name=".GerarActivity" />
    </application>

</manifest>